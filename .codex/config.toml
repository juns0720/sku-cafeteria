model = "gpt-5.5"

[profiles.dev-auto]
model = "gpt-5.5"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
approval_policy = "never"

[profiles.dev-auto.sandbox_workspace_write]
network_access = false

[profiles.analyze]
model = "gpt-5.5"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
approval_policy = "on-request"